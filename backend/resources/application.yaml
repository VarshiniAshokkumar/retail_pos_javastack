spring:
  application:
    name: rppro

  datasource:
    url: jdbc:mysql://localhost:3306/retail_pos_db
    username: root
    password: Varsh-7278
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080